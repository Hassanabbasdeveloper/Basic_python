# E-Commerce Discount Voucher

total = float(input("Enter total cart value: "))

if total > 5000:
    discount = total * 0.10
    final_bill = total - discount
    print("Discounted Total:", final_bill)
else:
    print("No discount applicable")